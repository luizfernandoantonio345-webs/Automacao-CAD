# ====================================================================
# grafana_datasources.yml - Datasources Provisioning
# Grafana lerá automaticamente ao iniciar
# ====================================================================

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
