{
  "version": 2,
  "buildCommand": "npm ls @mui/material 2>&1; CI=false npx react-scripts build 2>&1 || (echo BUILD_FAILED && exit 1)",
  "outputDirectory": "build",
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
